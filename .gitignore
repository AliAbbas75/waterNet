# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Secrets / environment files
.env
.env.*
!.env.example

# Credentials / keys
*.pem
*.key
*.p12
*.pfx
*.jks
*.keystore
.npmrc
!.npmrc.example

node_modules
/dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

tracking/

# Mobile app (Expo / React Native)
mobile/node_modules/
mobile/.expo/
mobile/.expo-shared/
mobile/.cache/
mobile/.parcel-cache/
mobile/dist/
mobile/web-build/
mobile/coverage/
mobile/android/
mobile/ios/

.claude/
__MACOSX/
# Besu node runtime state. RocksDB is a live database, not a build artifact:
# committing it produces snapshots that are corrupt on restore, and a stray
# `git clean`/`git restore` over these paths destroys a working chain.
# `data/key` is deliberately NOT ignored — it is the QBFT validator identity
# hard-coded into genesis.json's extraData; lose it and the node stops
# producing blocks.
infra/besu-network/data/*
!infra/besu-network/data/key

# Local engineering journal — working notes, not published.
docs/
