{
  "name": "waternet-blockchain",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "deploy:base-sepolia": "hardhat run scripts/deploy.js --network baseSepolia",
    "register:base-sepolia": "hardhat run scripts/registerUser.js --network baseSepolia",
    "setrole:base-sepolia": "hardhat run scripts/setRole.js --network baseSepolia",
    "revoke:base-sepolia": "hardhat run scripts/revokeUser.js --network baseSepolia"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@openzeppelin/contracts": "^5.0.2",
    "dotenv": "^16.6.1",
    "hardhat": "^2.22.17"
  }
}
